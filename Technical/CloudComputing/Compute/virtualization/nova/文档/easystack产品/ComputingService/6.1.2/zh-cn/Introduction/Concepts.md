---
title: 基本概念
---
 
* **云主机**

  运行在云环境上的虚拟机，相当于数据中心的一台物理服务器。用户可以通过选择合适的CPU、内存、操作系统、磁盘空间、网络和安全组等配置创建云主机。

* **云硬盘**

  为云主机提供块级存储设备，相当于一台物理机的硬盘。云硬盘是独立的资源，其生命周期独立于云主机，可以被挂载到任何云主机上，也可以从云主机卸载，然后挂载到其他云主机。

* **镜像**

  操作系统的安装模版。用户可以选择合适的操作系统镜像，创建所需要的云主机。 只有云管理员具有上传镜像操作权限，其他权限的用户只能使用和查看。但用户可以通过云主机快照创建新的镜像，并在启动云主机时选择”云主机快照”类型来使用新的镜像。

* **快照**

  用户可以对云主机创建快照，保存当时状态下的云主机数据作为备份。用户可以基于这个快照创建新的云主机。

* **物理节点**

  一个云环境中包含一组物理节点，每个物理节点对应一台物理服务器。物理节点可分为不同的角色，如控制节点、计算节点、存储节点和融合节点等。其中带计算角色的物理节点可以运行云主机。物理节点也可简称为“节点”。

* **SSH密钥对**

  基于密钥的安全验证登录方法，保证云主机安全。推荐使用密钥对登录云主机。

* **可用区**

  可以简单理解为一组节点的集合，这组节点具有独立的电力供应设备。比如一个独立供电的机房，一个独立供电的机架都可以被划分成可用区。所以，可用区主要是通过冗余来解决可用性问题，同时也提高容灾性和隔离性。

* **主机集合**

  可用区是一个面向用户的概念和功能，而主机集合是管理员用来根据硬件资源的某一属性来对硬件进行划分的功能，只对云管理员可见，其主要功能就是实现根据某一属性来划分物理机，比如按照地理位置，使用固态硬盘的机器，内存超过32G的机器，根据这些指标来构成一个主机集合。

* **云主机监控**

  用于监控云主机不同时间长度和时间精度的监控数据，以反映云主机运行状态，便于精准快速定位故障，实现高效运维。
  
  根据云主机是否已安装并启用Agent插件，实现对更多指标的监控，可以划分监控模式为“基础监控”和“详细监控”两种。各模式支持监控的指标，以及各指标的含义与统计方法如下（对于各监控模式，未标注统计方法则说明不支持对该指标进行监控）：


  <table>
  <thead>
      <tr>
        <th colspan="2">指标</th>
        <th>含义</th>
        <th>详细监控模式（启用Agent）</th>
        <th>基础监控模式（未启用Agent）</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td rowspan="4">CPU</td>
          <td>CPU使用率</td>
          <td>用于统计云主机的CPU使用率，以百分比为单位。</td>
          <td rowspan="4">* Linux：通过计算采集周期内/proc/stat中的变化获得。<br/>* Windows：通过typeperf中Processor(_Total)相关指标采集。</td>
          <td>通过Libvirt接口virDomainListGetStats获得。</td>
      </tr>
      <tr>
          <td>CPU使用率- User</td>
          <td>用于统计云主机的用户空间CPU使用率，以百分比为单位。</td>
          <td></td>
      </tr>
      <tr>
          <td>CPU使用率- System</td>
          <td>用于统计云主机的内核空间CPU使用率，以百分比为单位。</td>
          <td></td>
      </tr>
      <tr>
          <td>CPU使用率- Interrupt</td>
          <td>用于统计云主机的CPU中断时间占比，以百分比为单位。</td>
          <td></td>
      </tr>
      <tr>
          <td rowspan="4">内存</td>
          <td>剩余内存</td>
          <td>用于统计云主机剩余内存的使用量，以MiB为单位。</td>
          <td>* Linux：通过计算/proc/meminfo中的MemFree获得。<br/>* Windows：通过typeperf中Memory  Free指标采集。</td>
          <td></td>
      </tr>
      <tr>
          <td>可用内存</td>
          <td>用于统计云主机可用内存的使用量，以MiB为单位。</td>
          <td>* Linux：通过计算/proc/meminfo中的MemAvailable获得。<br/>* Windows：通过typeperf中Memory  Available MBytes指标采集。</td>
          <td></td>
      </tr>
      <tr>
          <td>已使用内存</td>
          <td>用于统计云主机已使用内存的使用量，以MiB为单位。</td>
          <td>通过计算(内存总量-可用内存)获得。</td>
          <td>通过计算(内存总量-可用内存)获得。其中，可用内存通过Libvirt接口 virDomainMemoryStats中字段usable获得。</td>
      </tr>
      <tr>
          <td>内存使用率</td>
          <td>用于统计云主机可用内存的使用率，以百分比为单位。</td>
          <td>通过计算(1-可用内存/内存总量)获得。</td>
          <td>通过计算(1-可用内存/内存总量)获得。</td>
      </tr>
      <tr>
          <td rowspan="6">磁盘</td>
          <td>磁盘使用量</td>
          <td>用于统计云主机的磁盘使用量，以GiB为单位。</td>
          <td>* Linux：通过计算分析(df-l)获得。<br/>* Windows：通过wmic logicaldisk获得。</td>
          <td></td>
      </tr>
      <tr>
          <td>磁盘使用率</td>
          <td>用于统计云主机的磁盘使用率，以百分比为单位。</td>
          <td>通过计算(1-磁盘使用量/磁盘总量)获得。</td>
          <td></td>
      </tr>
      <tr>
          <td>磁盘读/写IOPS-磁盘读IOPS</td>
          <td>用于统计云主机每块磁盘的读请求次数速率，以IOPS为单位。</td>
          <td rowspan="4">* Linux：通过计算采集周期内/proc/diskstats中的变化获得。<br/>* Windows：通过typeperf中PhysicalDisk Current Disk Queue Length指标采集。</td>
          <td></td>
      </tr>
      <tr>
          <td>磁盘读/写IOPS-磁盘写IOPS</td>
          <td>用于统计云主机每块磁盘的写请求次数速率，以IOPS为单位。</td>
          <td></td>
      </tr>
      <tr>
          <td>磁盘读/写速率-磁盘读请求速率</td>
          <td>用于统计云主机每块磁盘的读取数据的请求速率，以B/S为单位。</td>
          <td rowspan="2">通过Libvirt接口virDomainBlockStats获得。</td>
      </tr>
      <tr>
          <td>磁盘读/写速率-磁盘写请求速率</td>
          <td>用于统计云主机每块磁盘的写入数据的请求速率，以B/S为单位。</td>
      </tr>
      <tr>
          <td rowspan="4">网络</td>
          <td>网卡流量-网卡进流量</td>
          <td>用于统计云主机每块网卡的接收字节速率，以B/S为单位。</td>
          <td rowspan="4">* Linux：通过计算采集周期内/proc/net/dev中的变化获得。<br/>* Windows：通过GetIfEntry2获取每个网口的统计数据。</td>
          <td rowspan="2">通过Libvirt接口virDomainInterfaceStats获得。</td>
      </tr>
      <tr>
          <td>网卡流量-网卡出流量</td>
          <td>用于统计云主机每块网卡的发送字节速率，以B/S为单位。</td>
      </tr>
      <tr>
          <td>网卡进/出包速率-网卡进包速率</td>
          <td>用于统计云主机每块网卡的接收数据包速率，以PPS为单位。</td>
          <td></td>
      </tr>
      <tr>
          <td>网卡进/出包速率-网卡出包速率</td>
          <td>用于统计云主机每块网卡的发送数据包速率，以PPS为单位。</td>
          <td></td>
      </tr>
  </tbody></table>

